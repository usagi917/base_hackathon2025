# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.key
*.crt
*.cer
*.der
*.csr
*.p12
*.pfx
*.jks
*.keystore

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log
.eslintcache

# env files (can opt-in for committing if needed)
.env*
!.env.example
!.env.sample

# local credential/config files (often contain tokens)
# NOTE: .gitignore doesn't stop already-tracked files from being committed.
.npmrc
!.npmrc.example
.yarnrc.yml
!.yarnrc.yml.example
.pnpmrc
!.pnpmrc.example

# common local auth files
.netrc

# cloud/provider credentials (never commit)
.aws/
.azure/
.kube/
.docker/config.json
.docker/config.json.*

# direnv
.envrc
.direnv/

# local secrets vaults / encrypted blobs
.secrets/
secrets/
*.age
*.gpg
*.kdbx

# terraform state/vars (often contain secrets)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.auto.tfvars
*.auto.tfvars.json
!*.tfvars.example
!*.auto.tfvars.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# build artifacts (contracts)
contracts/cache/
contracts/out/
contracts/broadcast/

# editor
.vscode/
.idea/
*.swp
*.swo

# local assistant transcripts / backups
.amplifier/
